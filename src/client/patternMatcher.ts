/**
 * PatternMatcher – re-exported from src/shared/patternMatcher.ts.
 *
 * The implementation lives in shared/ so it can be used in both browser and
 * Node.js (virtual / simulation) contexts without any camera or DOM dependency.
 */
export { PatternMatcher } from '../shared/patternMatcher.js';
export type { MatchResult } from '../shared/patternMatcher.js';
