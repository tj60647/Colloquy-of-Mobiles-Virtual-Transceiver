/**
 * MatcherInputAdapter – re-exported from src/shared/matcherInputAdapter.ts.
 *
 * The implementation lives in shared/ so it can be used in both browser and
 * Node.js (virtual / simulation) contexts without any camera or DOM dependency.
 */
export { MatcherInputAdapter } from '../shared/matcherInputAdapter.js';
export type { MatcherTick } from '../shared/matcherInputAdapter.js';
